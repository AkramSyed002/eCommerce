import { SearchByProductsContainer } from "@muc/modules";
import React from "react";

const SearchProducts = () => {
  return (
    <React.Fragment>
      <SearchByProductsContainer />
    </React.Fragment>
  );
};

export default SearchProducts;
